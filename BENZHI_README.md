# ConfigHub

ConfigHub 是面向企业服务集群的分布式配置中心。配置按命名空间隔离，配置项
带版本号以文件形式持久化；草稿经校验后发布成新版本，发布成功后推送给订阅端，
客户端按快照拉取；发布出错可回滚，配置总量受配额约束，所有变更留审计。

## 构建

```bash
docker build -t confighub .
```

## 运行

```bash
go run ./cmd/confighub -addr 127.0.0.1:7790 -data ./data -namespaces 4 -quota 1048576
```

启动后打开 http://127.0.0.1:7790 即可进入控制台，页面包含配置项、发布、
订阅和审计日志四个视图，JSON API 位于 /api 下。

## 数据目录

每个命名空间在数据目录下保存自己的写入日志、提交水位、删除墓碑与元数据；
版本账本、快照代际、订阅游标、确认记录、审计日志和配额账本也在数据目录内
持久化。数据目录可以直接整体备份。
